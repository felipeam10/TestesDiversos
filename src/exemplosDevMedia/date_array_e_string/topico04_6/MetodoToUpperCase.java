package exemplosDevMedia.date_array_e_string.topico04_6;

public class MetodoToUpperCase {

    public static void main(String[] args) {

        String s1 = "rodolfo rodrigues";
        String s2 = "Alfredo Augusto";
        String s3 = "JONNY DEBRUCE";
        System.out.println(s1.toUpperCase());
        System.out.println(s2.toUpperCase());
        System.out.println(s3.toUpperCase());
    }
}

/**
 * O método toUpperCase() retorna uma nova String com o mesmo conteúdo da original, só que com todos os caracteres em letras maiúsculas.
 * Assim como o método trim(), toUpperCase() também pode ser utilizado antes de salvar dados de um formulário na base de dados.
 */
